package com.example.saferouter.ui.theme

import androidx.compose.ui.graphics.Color

// Colores principales
val PrimaryBlue = Color(0xFF2563EB) // Azul principal (botones, elementos interactivos)
val PrimaryBlueLight = Color(0xFF60A5FA) // Azul claro
val PrimaryBlueDark = Color(0xFF1E40AF) // Azul oscuro

// Colores de fondo
val BackgroundLight = Color(0xFFF8FAFC) // Fondo claro
val BackgroundWhite = Color(0xFFFFFFFF) // Blanco puro
val SurfaceLight = Color(0xFFF1F5F9) // Superficie clara

// Colores de texto
val TextPrimary = Color(0xFF1E293B) // Texto principal (negro suave)
val TextSecondary = Color(0xFF64748B) // Texto secundario (gris)
val TextTertiary = Color(0xFF94A3B8) // Texto terciario (gris claro)

// Colores de alertas y reportes
val AlertRed = Color(0xFFEF4444) // Rojo para alertas de peligro
val AlertRedLight = Color(0xFFFEE2E2) // Fondo rojo claro
val AlertRedBg = Color(0xFFFECDD3) // Fondo de tarjeta de robo

val WarningYellow = Color(0xFFF59E0B) // Amarillo para advertencias
val WarningYellowLight = Color(0xFFFEF3C7) // Fondo amarillo claro
val WarningYellowBg = Color(0xFFFDE68A) // Fondo de tarjeta de advertencia

val InfoBlue = Color(0xFF3B82F6) // Azul para información
val InfoBlueLight = Color(0xFFDCECFE) // Fondo azul claro
val InfoBlueBg = Color(0xFFBFDBFE) // Fondo de tarjeta de información

val SuccessGreen = Color(0xFF10B981) // Verde para éxito
val SuccessGreenLight = Color(0xFFD1FAE5) // Fondo verde claro
val SuccessGreenBg = Color(0xFFA7F3D0) // Fondo de tarjeta de éxito

// Colores de iconos específicos
val IconWarning = Color(0xFFEAB308) // Icono de advertencia (bombilla)
val IconDanger = Color(0xFFDC2626) // Icono de peligro
val IconPolice = Color(0xFF3B82F6) // Icono de policía
val IconSuccess = Color(0xFF059669) // Icono de verificación

// Colores de bordes y divisores
val BorderLight = Color(0xFFE2E8F0) // Borde claro
val BorderMedium = Color(0xFFCBD5E1) // Borde medio
val Divider = Color(0xFFE5E7EB) // Divisor

// Colores para modo oscuro (opcional)
val DarkBackground = Color(0xFF0F172A) // Fondo oscuro
val DarkSurface = Color(0xFF1E293B) // Superficie oscura
val DarkTextPrimary = Color(0xFFF1F5F9) // Texto claro en modo oscuro

// Colores de gráficos (para el dashboard)
val ChartPurple = Color(0xFF8B5CF6)
val ChartBlue = Color(0xFF3B82F6)
val ChartGreen = Color(0xFF10B981)
val ChartYellow = Color(0xFFF59E0B)
val ChartRed = Color(0xFFEF4444)
val ChartPink = Color(0xFFEC4899)
val ChartOrange = Color(0xFFF97316)

// Gradientes (definidos como pares de colores)
val GradientStart = Color(0xFF60A5FA) // Inicio del gradiente azul
val GradientEnd = Color(0xFF2563EB) // Fin del gradiente azul

// Colores de notificaciones
val NotificationBadge = Color(0xFFEF4444) // Badge rojo para notificaciones
val NotificationBorder = Color(0xFF3B82F6) // Borde azul de notificaciones activas

// Colores deshabilitados
val DisabledGray = Color(0xFF9CA3AF) // Elementos deshabilitados
val DisabledBackground = Color(0xFFF3F4F6) // Fondo deshabilitado